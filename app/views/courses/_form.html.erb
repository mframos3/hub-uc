<%= form_with(model: course, local: true) do |form| %>
  <% if course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course.errors.count, "error") %> prohibited this course from being saved:</h2>
      <ul>
      <% course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="panel panel-default">
  <div class="panel-body">
  <div class="form-inputs">

  <div class="form-group">
    <%= form.label :Nombre %>
    <%= form.text_field :name, class: "form-control", id: :course_name, :required => 'required' %>
  </div>

  <div class="form-group">
    <%= form.label :Sigla %>
    <%= form.text_field :initials, class: "form-control", id: :course_initials, :required => 'required' %>
  </div>
  <div class="actions">
    <% if @editing %>
      <%= form.submit "Actualizar Curso", class: "btn btn-primary" %>
    <% else %>
      <%= form.submit "Añadir Curso", class: "btn btn-primary" %>
    <% end %>
  </div>
  </div>
  </div>
  </div>
<% end %>
