<h1>Enviar Solicitud De Moderador</h1>
<%if !current_user%>
    <% raise CanCan::AccessDenied.new("Not authorized!",:create, ModeratorRequest) %>
<%end%>

<%= render 'form', moderator_request: @moderator_request %>
<br>
<div class="btn-group">
  <%= link_to 'Volver', course_path(@course), class: "btn btn-default" %>
</div>
